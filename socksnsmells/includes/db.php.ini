<?php
$servername = 'socks-store-mysql';
$username = 'socksuser';
$password = 'another_secret_pwd';
$db = $_ENV["MYSQL_DATABASE"];

$conn = new mysqli($servername, $username, $password, $db);

if ($conn->connect_error) {
  die("Connection failed: " . $conn->connect_error);
}

if (isset($_GET['id'])) {
  $sql = "SELECT id, name, brand, variant, color, price, inventory FROM socks WHERE id=" . $_GET['id'];
  $result = $conn->query($sql);
  $row = $result->fetch_assoc();
}
?>
